import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-operators",
  title: "Operators: LIKE, IN, BETWEEN",
  category: "sql-querying",
  difficulty: "mid",
  order: 3,
  summary: "Express richer WHERE conditions with ranges (BETWEEN), membership (IN), and pattern matching (LIKE).",
  est: 10,
  tags: ["LIKE", "IN", "BETWEEN", "operators", "patterns"],
};

export default meta;
