import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftui-layout",
  title: "Layout System",
  category: "ui",
  difficulty: "mid",
  order: 5,
  summary: "The parent-proposes / child-chooses sizing negotiation, stacks, frames, GeometryReader, and the Layout protocol.",
  est: 14,
  tags: ["SwiftUI", "layout", "GeometryReader", "frame", "alignment"],
};

export default meta;
