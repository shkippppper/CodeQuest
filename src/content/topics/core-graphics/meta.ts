import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "core-graphics",
  title: "Core Graphics & Custom Drawing",
  category: "ui",
  difficulty: "senior",
  order: 24,
  summary:
    "Drawing custom content by hand: the graphics context, paths, fills and gradients, coordinate systems, image rendering, and when NOT to use it.",
  est: 13,
  tags: ["core-graphics", "drawing", "cgcontext", "canvas"],
};

export default meta;
