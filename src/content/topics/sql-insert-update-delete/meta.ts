import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-insert-update-delete",
  title: "INSERT, UPDATE & DELETE",
  category: "sql-dml-transactions",
  difficulty: "junior",
  order: 1,
  summary: "Add rows with INSERT, change them with UPDATE, remove them with DELETE — and why WHERE is critical.",
  est: 10,
  tags: ["INSERT", "UPDATE", "DELETE", "upsert"],
};

export default meta;
