import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-constraints",
  title: "Constraints",
  category: "sql-ddl",
  difficulty: "mid",
  order: 2,
  summary: "Rules the database enforces on your data — PRIMARY KEY, FOREIGN KEY, UNIQUE, NOT NULL, CHECK, DEFAULT.",
  est: 11,
  tags: ["constraints", "UNIQUE", "CHECK", "NOT NULL", "DEFAULT"],
};

export default meta;
