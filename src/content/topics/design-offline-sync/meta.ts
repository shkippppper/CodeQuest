import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "design-offline-sync",
  title: "Designing Offline-First Sync",
  category: "system-design",
  difficulty: "senior",
  order: 4,
  summary: "Design an offline-first sync engine: local source of truth, change tracking, conflict resolution, retry and idempotency, and consistency guarantees.",
  est: 18,
  tags: ["offline-first", "sync engine", "conflict resolution", "idempotency", "consistency"],
};

export default meta;
