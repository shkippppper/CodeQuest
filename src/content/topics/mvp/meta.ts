import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "mvp",
  title: "MVP",
  category: "architecture",
  difficulty: "mid",
  order: 3,
  summary: "A Presenter drives a passive View through a protocol contract — explicit, testable, no binding required.",
  est: 11,
  tags: ["MVP", "Presenter", "passive view", "protocol", "testability"],
};

export default meta;
