import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "rendering-performance",
  title: "Rendering Performance & Hitches",
  category: "performance",
  difficulty: "senior",
  order: 4,
  summary: "The frame budget, offscreen rendering, overdraw, and how to find and fix hitches in SwiftUI and UIKit.",
  est: 14,
  tags: ["frame budget", "offscreen rendering", "overdraw", "hitches", "Core Animation"],
};

export default meta;
