import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "ds-stacks-queues",
  title: "Stacks & Queues",
  category: "cs-fundamentals",
  difficulty: "junior",
  order: 4,
  summary: "LIFO and FIFO order, building both on top of an array in Swift, and the monotonic stack pattern.",
  est: 12,
  tags: ["stack", "queue", "deque", "monotonic stack", "LIFO", "FIFO"],
};

export default meta;
