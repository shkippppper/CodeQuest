import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-subqueries",
  title: "Subqueries",
  category: "sql-subqueries",
  difficulty: "mid",
  order: 1,
  summary: "Nest one query inside another — as a single value, a list for IN, or a derived table in FROM.",
  est: 11,
  tags: ["subquery", "scalar", "IN", "derived table"],
};

export default meta;
