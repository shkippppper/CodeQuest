import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "unidirectional",
  title: "Unidirectional Data Flow / Redux",
  category: "architecture",
  difficulty: "senior",
  order: 7,
  summary: "One-way data flow — single source of truth, actions, reducers, and side effects in middleware.",
  est: 12,
  tags: ["unidirectional", "Redux", "single source of truth", "reducer", "middleware"],
};

export default meta;
