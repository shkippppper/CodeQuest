import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "rest-json",
  title: "REST, JSON & Codable Pipelines",
  category: "foundation",
  difficulty: "mid",
  order: 2,
  summary: "Structuring a request layer around endpoints, Codable models, mapped errors, retries, and mockable networking.",
  est: 14,
  tags: ["REST", "JSON", "Codable", "networking", "error handling", "mocking"],
};

export default meta;
