import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "test-doubles",
  title: "Test Doubles",
  category: "testing",
  difficulty: "mid",
  order: 3,
  summary: "Dummies, stubs, spies, mocks, and fakes — how to isolate code under test with protocol-based doubles written by hand in Swift.",
  est: 11,
  tags: ["test doubles", "stub", "mock", "spy", "fake", "dependency injection", "XCTest"],
};

export default meta;
