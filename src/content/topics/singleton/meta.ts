import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "singleton",
  title: "Singleton",
  category: "patterns",
  difficulty: "junior",
  order: 1,
  summary: "One shared instance, guaranteed by the type system — and why that guarantee usually costs you more than it saves.",
  est: 9,
  tags: ["singleton", "creational", "shared instance", "thread safety", "testability"],
};

export default meta;
