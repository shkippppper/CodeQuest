import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "strategy",
  title: "Strategy",
  category: "patterns",
  difficulty: "mid",
  order: 11,
  summary: "Swapping an algorithm at runtime behind a shared interface, with protocols and closures as the two Swift-native shapes.",
  est: 10,
  tags: ["strategy", "protocol", "closure", "runtime polymorphism"],
};

export default meta;
