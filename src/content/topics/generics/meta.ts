import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "generics",
  title: "Generics",
  category: "types",
  difficulty: "mid",
  order: 2,
  summary: "Write flexible, type-safe code once for many types — with constraints, conditional conformance, and specialization.",
  est: 14,
  tags: ["generics", "type constraints", "where", "conditional conformance", "specialization"],
};

export default meta;
