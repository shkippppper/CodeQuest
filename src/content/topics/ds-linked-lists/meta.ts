import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "ds-linked-lists",
  title: "Linked Lists",
  category: "cs-fundamentals",
  difficulty: "mid",
  order: 3,
  summary: "Node-based sequences: singly vs doubly, pointer reversal, and fast/slow cycle detection.",
  est: 13,
  tags: ["linked list", "node", "reversal", "fast slow pointers", "cycle detection"],
};

export default meta;
