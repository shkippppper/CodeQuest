import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "cs-concurrency",
  title: "Concurrency Primitives",
  category: "cs-fundamentals",
  difficulty: "senior",
  order: 12,
  summary: "Locks, semaphores, atomicity, and the three classic failure modes — worked through the producer-consumer problem.",
  est: 15,
  tags: ["locks", "mutex", "semaphore", "atomicity", "deadlock", "livelock", "starvation", "producer-consumer"],
};

export default meta;
