import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "functions-closures",
  title: "Functions & Closures",
  category: "language",
  difficulty: "junior",
  order: 5,
  summary: "Argument labels, defaults, inout, closures, trailing-closure syntax, escaping, and capturing.",
  est: 14,
  tags: ["functions", "closures", "escaping", "capture", "trailing closure"],
};

export default meta;
