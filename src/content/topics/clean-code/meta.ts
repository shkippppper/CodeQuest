import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "clean-code",
  title: "Clean Code Practices",
  category: "solid",
  difficulty: "mid",
  order: 7,
  summary: "Naming, small functions, guard clauses, and the other everyday habits that make code readable by someone other than its author.",
  est: 12,
  tags: ["clean code", "naming", "readability", "guard clauses", "command-query separation"],
};

export default meta;
