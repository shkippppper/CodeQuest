import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "clean-architecture",
  title: "Clean Architecture",
  category: "architecture",
  difficulty: "senior",
  order: 5,
  summary: "Concentric layers and the dependency rule — business logic at the center, frameworks at the edge.",
  est: 14,
  tags: ["clean architecture", "use cases", "dependency rule", "layers", "boundaries"],
};

export default meta;
