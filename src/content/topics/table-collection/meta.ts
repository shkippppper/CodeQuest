import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "table-collection",
  title: "UITableView & UICollectionView",
  category: "ui",
  difficulty: "mid",
  order: 15,
  summary: "Data source & delegate, cell reuse, sizing, and the performance rules behind smooth scrolling lists.",
  est: 14,
  tags: ["UIKit", "UITableView", "UICollectionView", "cell reuse", "data source"],
};

export default meta;
