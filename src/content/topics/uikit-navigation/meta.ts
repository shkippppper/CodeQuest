import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "uikit-navigation",
  title: "Navigation & Coordinators (UIKit)",
  category: "ui",
  difficulty: "mid",
  order: 18,
  summary: "UINavigationController push/pop, modal presentation, segues vs programmatic, and passing data back.",
  est: 12,
  tags: ["UIKit", "UINavigationController", "modal", "segue", "delegate"],
};

export default meta;
