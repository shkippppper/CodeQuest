import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "state-pattern",
  title: "State Pattern",
  category: "patterns",
  difficulty: "senior",
  order: 13,
  summary: "Modeling an object whose behavior changes with its status, from boolean flags to enum state machines to swappable State objects.",
  est: 12,
  tags: ["state", "state machine", "enum", "behavioral pattern"],
};

export default meta;
