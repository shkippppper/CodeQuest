import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sqlite",
  title: "SQLite & GRDB",
  category: "foundation",
  difficulty: "senior",
  order: 8,
  summary: "When raw SQL beats an ORM, and how GRDB adds type-safe records, observation, and migrations on top of SQLite.",
  est: 14,
  tags: ["SQLite", "GRDB", "SQL", "migrations", "persistence"],
};

export default meta;
