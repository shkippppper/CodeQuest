import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-order-limit",
  title: "ORDER BY, LIMIT & DISTINCT",
  category: "sql-querying",
  difficulty: "junior",
  order: 2,
  summary: "Sort results with ORDER BY, take the top rows with LIMIT/OFFSET, and drop duplicates with DISTINCT.",
  est: 10,
  tags: ["ORDER BY", "LIMIT", "OFFSET", "DISTINCT"],
};

export default meta;
