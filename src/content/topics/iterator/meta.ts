import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "iterator",
  title: "Iterator Pattern",
  category: "patterns",
  difficulty: "mid",
  order: 14,
  summary: "How for-in actually works: IteratorProtocol, Sequence, building custom iterators, and lazy sequences.",
  est: 10,
  tags: ["iterator", "Sequence", "IteratorProtocol", "lazy", "for-in"],
};

export default meta;
