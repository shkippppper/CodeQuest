import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "design-image-loader",
  title: "Designing an Image-Loading Library",
  category: "system-design",
  difficulty: "senior",
  order: 3,
  summary: "Build an image-loading library from its API surface down: memory and disk caches, cancellation, decoding and downsampling, prefetch, and concurrency.",
  est: 18,
  tags: ["image loading", "caching", "cancellation", "downsampling", "concurrency"],
};

export default meta;
