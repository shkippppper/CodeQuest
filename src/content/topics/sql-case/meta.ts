import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-case",
  title: "CASE Expressions",
  category: "sql-querying",
  difficulty: "mid",
  order: 4,
  summary: "Return a value that depends on conditions — SQL's if/else — usable in SELECT, ORDER BY, and aggregates.",
  est: 10,
  tags: ["CASE", "conditional", "WHEN", "aggregation"],
};

export default meta;
