import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "ocp",
  title: "Open/Closed Principle",
  category: "solid",
  difficulty: "mid",
  order: 2,
  summary: "Extending behavior through protocols and polymorphism instead of editing working code with new if/switch branches.",
  est: 11,
  tags: ["SOLID", "OCP", "protocols", "polymorphism", "strategy"],
};

export default meta;
