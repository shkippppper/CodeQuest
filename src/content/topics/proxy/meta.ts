import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "proxy",
  title: "Proxy",
  category: "patterns",
  difficulty: "senior",
  order: 7,
  summary: "A stand-in object that controls access to the real thing — lazily, defensively, or across a boundary.",
  est: 12,
  tags: ["proxy", "lazy loading", "virtual proxy", "protection proxy", "remote proxy"],
};

export default meta;
