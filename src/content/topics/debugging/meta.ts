import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "debugging",
  title: "Debugging with LLDB",
  category: "tooling",
  difficulty: "mid",
  order: 6,
  summary: "Symbolic and conditional breakpoints, core LLDB commands, po/expr, watchpoints, and the view hierarchy debugger.",
  est: 11,
  tags: ["LLDB", "breakpoints", "debugging", "watchpoints", "Xcode"],
};

export default meta;
