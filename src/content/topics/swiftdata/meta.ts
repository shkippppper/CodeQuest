import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftdata",
  title: "SwiftData",
  category: "foundation",
  difficulty: "mid",
  order: 7,
  summary: "How @Model, ModelContainer/ModelContext, and @Query replace Core Data's boilerplate while sharing its underlying persistence model.",
  est: 13,
  tags: ["SwiftData", "@Model", "ModelContainer", "@Query", "migration"],
};

export default meta;
