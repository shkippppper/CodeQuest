import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "xctest",
  title: "Unit Testing with XCTest",
  category: "testing",
  difficulty: "mid",
  order: 1,
  summary: "How XCTest structures a test, asserts on outcomes, manages setup/teardown, and waits for async work.",
  est: 14,
  tags: ["XCTest", "unit testing", "assertions", "AAA", "XCTestExpectation"],
};

export default meta;
