import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "observer",
  title: "Observer",
  category: "patterns",
  difficulty: "mid",
  order: 10,
  summary: "Broadcasting state changes to many listeners: NotificationCenter, KVO, and Combine/Observation as the modern observer.",
  est: 11,
  tags: ["observer", "NotificationCenter", "KVO", "Combine", "Observation"],
};

export default meta;
