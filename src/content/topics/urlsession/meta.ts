import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "urlsession",
  title: "URLSession & Networking",
  category: "foundation",
  difficulty: "mid",
  order: 1,
  summary: "Fetching data over the network with URLSession: configurations, task types, async/await, and handling failures.",
  est: 14,
  tags: ["URLSession", "networking", "async/await", "HTTP", "authentication"],
};

export default meta;
