import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "control-flow",
  title: "Control Flow & Pattern Matching",
  category: "language",
  difficulty: "junior",
  order: 4,
  summary: "if / guard / switch, exhaustive matching, where clauses, and Swift's powerful pattern matching.",
  est: 12,
  tags: ["if", "guard", "switch", "pattern matching", "where"],
};

export default meta;
