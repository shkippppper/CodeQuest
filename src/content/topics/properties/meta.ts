import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "properties",
  title: "Properties",
  category: "language",
  difficulty: "junior",
  order: 8,
  summary: "Stored vs computed properties, willSet/didSet observers, lazy, and static/type properties.",
  est: 12,
  tags: ["stored", "computed", "lazy", "didSet", "static"],
};

export default meta;
