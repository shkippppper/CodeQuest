import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-set-operations",
  title: "UNION, INTERSECT & EXCEPT",
  category: "sql-joins",
  difficulty: "mid",
  order: 3,
  summary: "Stack or compare the results of two queries as sets — combine, intersect, or subtract rows.",
  est: 10,
  tags: ["UNION", "INTERSECT", "EXCEPT", "set operations"],
};

export default meta;
