import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "continuations",
  title: "Continuations",
  category: "concurrency",
  difficulty: "senior",
  order: 9,
  summary: "Bridging completion-handler callbacks into async/await — and the resume-exactly-once rule.",
  est: 12,
  tags: ["continuation", "withCheckedContinuation", "callback bridging", "async"],
};

export default meta;
