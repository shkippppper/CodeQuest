import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-select-where",
  title: "SELECT & WHERE",
  category: "sql-querying",
  difficulty: "junior",
  order: 1,
  summary: "Read specific columns with SELECT and keep only the rows you want with WHERE.",
  est: 11,
  tags: ["SELECT", "WHERE", "filtering", "query"],
};

export default meta;
