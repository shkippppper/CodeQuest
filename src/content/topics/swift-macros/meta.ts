import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swift-macros",
  title: "Swift Macros",
  category: "language",
  difficulty: "senior",
  order: 13,
  summary:
    "How macros generate code at compile time — freestanding vs attached, the two-module setup, and why they're safer than the old workarounds.",
  est: 14,
  tags: ["macros", "metaprogramming", "compile-time", "swiftsyntax"],
};

export default meta;
