import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "ds-arrays",
  title: "Arrays & Strings",
  category: "cs-fundamentals",
  difficulty: "junior",
  order: 2,
  summary: "Array internals and the two-pointer / sliding-window patterns that solve most array & string problems.",
  est: 13,
  tags: ["array", "string", "two pointers", "sliding window", "in-place"],
};

export default meta;
