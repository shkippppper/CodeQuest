import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-tables-keys",
  title: "Tables, Rows & Keys",
  category: "sql-foundations",
  difficulty: "junior",
  order: 2,
  summary: "Primary keys give each row a unique name; foreign keys link tables and keep references honest.",
  est: 11,
  tags: ["primary key", "foreign key", "referential integrity", "tables"],
};

export default meta;
