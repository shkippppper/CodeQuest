import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "views-layers",
  title: "Views & Layers (Core Animation)",
  category: "ui",
  difficulty: "mid",
  order: 19,
  summary: "Every UIView is backed by a CALayer — the layer draws and animates; the view handles events and layout.",
  est: 12,
  tags: ["UIKit", "CALayer", "Core Animation", "implicit animation", "CABasicAnimation"],
};

export default meta;
