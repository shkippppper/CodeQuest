import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "adapter",
  title: "Adapter",
  category: "patterns",
  difficulty: "mid",
  order: 4,
  summary: "Wrapping an interface your code doesn't control so it matches the protocol your code does — object adapters, protocol conformance, and third-party APIs.",
  est: 9,
  tags: ["adapter", "structural", "protocol conformance", "wrapper", "third-party"],
};

export default meta;
