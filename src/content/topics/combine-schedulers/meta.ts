import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "combine-schedulers",
  title: "Schedulers & Threading",
  category: "reactive",
  difficulty: "senior",
  order: 4,
  summary: "Controlling which thread a Combine pipeline runs and delivers on with receive(on:) and subscribe(on:).",
  est: 13,
  tags: ["Combine", "Scheduler", "receive(on:)", "subscribe(on:)", "threading"],
};

export default meta;
