import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "basics-types",
  title: "Variables, Constants & Type Inference",
  category: "language",
  difficulty: "junior",
  order: 1,
  summary: "let vs var, how type inference works, type safety, numeric conversions, and tuples.",
  est: 11,
  tags: ["let", "var", "type inference", "tuples", "type safety"],
};

export default meta;
