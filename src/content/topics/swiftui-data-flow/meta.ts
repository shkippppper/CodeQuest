import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftui-data-flow",
  title: "Data Flow (ObservableObject & Environment)",
  category: "ui",
  difficulty: "mid",
  order: 3,
  summary: "Sharing reference-type models: ObservableObject, @StateObject vs @ObservedObject, and the Environment.",
  est: 14,
  tags: ["ObservableObject", "@StateObject", "@ObservedObject", "@EnvironmentObject", "@Published"],
};

export default meta;
