import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "capture-lists",
  title: "Capture Lists in Closures",
  category: "memory",
  difficulty: "mid",
  order: 4,
  summary: "How closures capture references, and using [weak self] / [unowned self] to control ownership.",
  est: 12,
  tags: ["capture list", "weak self", "unowned self", "closure", "guard let self"],
};

export default meta;
