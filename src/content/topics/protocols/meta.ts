import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "protocols",
  title: "Protocols & Protocol-Oriented Programming",
  category: "types",
  difficulty: "mid",
  order: 1,
  summary: "Defining contracts, default implementations via extensions, and Swift's protocol-first design over class inheritance.",
  est: 14,
  tags: ["protocol", "POP", "default implementation", "composition", "conformance"],
};

export default meta;
