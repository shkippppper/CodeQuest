import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "codable",
  title: "Codable",
  category: "types",
  difficulty: "mid",
  order: 9,
  summary: "Type-safe JSON encoding/decoding via synthesized Codable, CodingKeys, custom coding, and strategies.",
  est: 14,
  tags: ["Codable", "Encodable", "Decodable", "CodingKeys", "JSONDecoder"],
};

export default meta;
