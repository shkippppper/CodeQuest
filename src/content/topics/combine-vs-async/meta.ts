import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "combine-vs-async",
  title: "Combine vs async/await",
  category: "reactive",
  difficulty: "senior",
  order: 6,
  summary: "When a Publisher's stream of values still beats async/await's single result, and how to bridge or migrate between them.",
  est: 12,
  tags: ["Combine", "async/await", "AsyncSequence", ".values", "migration"],
};

export default meta;
