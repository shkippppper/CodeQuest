import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "collections",
  title: "Collections: Array, Set, Dictionary",
  category: "language",
  difficulty: "junior",
  order: 2,
  summary: "Swift's three core collections — when to reach for each, their value semantics, and map/filter/reduce.",
  est: 13,
  tags: ["array", "set", "dictionary", "map", "filter", "reduce"],
};

export default meta;
