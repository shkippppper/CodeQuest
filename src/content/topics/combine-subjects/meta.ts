import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "combine-subjects",
  title: "Subjects & Multicasting",
  category: "reactive",
  difficulty: "senior",
  order: 3,
  summary: "Pushing values into Combine by hand with PassthroughSubject and CurrentValueSubject, and sharing one subscription across many subscribers.",
  est: 13,
  tags: ["Combine", "PassthroughSubject", "CurrentValueSubject", "share", "backpressure"],
};

export default meta;
