import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-having",
  title: "HAVING vs WHERE",
  category: "sql-aggregation",
  difficulty: "mid",
  order: 2,
  summary: "WHERE filters rows before grouping; HAVING filters whole groups after aggregation.",
  est: 9,
  tags: ["HAVING", "WHERE", "GROUP BY", "filtering"],
};

export default meta;
