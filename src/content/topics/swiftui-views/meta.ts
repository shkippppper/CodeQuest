import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftui-views",
  title: "Views & Declarative UI",
  category: "ui",
  difficulty: "junior",
  order: 1,
  summary: "The View protocol, body composition, and why SwiftUI views are cheap value types you describe, not objects you mutate.",
  est: 12,
  tags: ["SwiftUI", "View", "body", "declarative", "ViewBuilder"],
};

export default meta;
