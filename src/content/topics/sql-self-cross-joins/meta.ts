import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-self-cross-joins",
  title: "Self & Cross Joins",
  category: "sql-joins",
  difficulty: "mid",
  order: 2,
  summary: "Join a table to itself with aliases (self join), or pair every row with every other (cross join).",
  est: 10,
  tags: ["self join", "cross join", "cartesian product", "alias"],
};

export default meta;
