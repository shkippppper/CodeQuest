import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-window-functions",
  title: "Window Functions",
  category: "sql-aggregation",
  difficulty: "senior",
  order: 3,
  summary: "Compute per-group values without collapsing rows — running totals, rankings, and per-partition aggregates.",
  est: 13,
  tags: ["window", "OVER", "PARTITION BY", "ROW_NUMBER", "RANK"],
};

export default meta;
