import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "access-control",
  title: "Access Control",
  category: "language",
  difficulty: "mid",
  order: 12,
  summary: "The five access levels, private vs fileprivate, open vs public, module boundaries, and @testable.",
  est: 11,
  tags: ["access control", "private", "public", "open", "internal"],
};

export default meta;
