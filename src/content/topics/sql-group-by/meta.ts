import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-group-by",
  title: "GROUP BY & Aggregates",
  category: "sql-aggregation",
  difficulty: "mid",
  order: 1,
  summary: "Collapse many rows into per-group summaries with COUNT, SUM, AVG, MIN, MAX and GROUP BY.",
  est: 11,
  tags: ["GROUP BY", "COUNT", "SUM", "AVG", "aggregate"],
};

export default meta;
