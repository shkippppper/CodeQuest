import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftui-lists",
  title: "Lists & Performance",
  category: "ui",
  difficulty: "mid",
  order: 6,
  summary: "List, ForEach, and the identity that drives diffing — plus lazy containers and performance pitfalls.",
  est: 13,
  tags: ["SwiftUI", "List", "ForEach", "identity", "lazy", "performance"],
};

export default meta;
