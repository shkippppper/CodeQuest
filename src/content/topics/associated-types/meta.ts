import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "associated-types",
  title: "Associated Types & Type Erasure",
  category: "types",
  difficulty: "senior",
  order: 3,
  summary: "Generic protocols via associatedtype, the Self/associated-type limitation, and erasing types with an eraser.",
  est: 14,
  tags: ["associatedtype", "type erasure", "Self requirement", "AnySequence", "PATs"],
};

export default meta;
