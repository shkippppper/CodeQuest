import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "ds-hash-tables",
  title: "Hash Tables",
  category: "cs-fundamentals",
  difficulty: "mid",
  order: 5,
  summary: "How Set and Dictionary get O(1) average lookups: hashing, buckets, and collision handling underneath.",
  est: 13,
  tags: ["hash table", "hashable", "collision", "buckets", "load factor"],
};

export default meta;
