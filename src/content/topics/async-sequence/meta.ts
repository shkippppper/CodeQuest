import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "async-sequence",
  title: "AsyncSequence & AsyncStream",
  category: "concurrency",
  difficulty: "senior",
  order: 8,
  summary: "Iterating values that arrive over time with for-await, and bridging callbacks into AsyncStream.",
  est: 13,
  tags: ["AsyncSequence", "AsyncStream", "for await", "backpressure", "streaming"],
};

export default meta;
