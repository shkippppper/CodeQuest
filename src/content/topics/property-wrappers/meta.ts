import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "property-wrappers",
  title: "Property Wrappers",
  category: "types",
  difficulty: "mid",
  order: 6,
  summary: "Reusable get/set logic attached to a property via @Wrapper, with wrappedValue and the $ projectedValue.",
  est: 13,
  tags: ["property wrapper", "wrappedValue", "projectedValue", "@State", "reuse"],
};

export default meta;
