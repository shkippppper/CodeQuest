import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "enums",
  title: "Enums & Associated Values",
  category: "language",
  difficulty: "junior",
  order: 6,
  summary: "Swift enums as first-class types — raw values, associated values, pattern matching, and state machines.",
  est: 12,
  tags: ["enum", "associated values", "raw values", "CaseIterable", "pattern matching"],
};

export default meta;
