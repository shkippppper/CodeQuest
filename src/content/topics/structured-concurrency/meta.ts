import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "structured-concurrency",
  title: "Structured Concurrency",
  category: "concurrency",
  difficulty: "senior",
  order: 4,
  summary: "Task trees, async let, TaskGroup, and how cancellation propagates through the hierarchy.",
  est: 15,
  tags: ["Task", "async let", "TaskGroup", "cancellation", "structured"],
};

export default meta;
