import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-data-types",
  title: "Data Types",
  category: "sql-foundations",
  difficulty: "junior",
  order: 4,
  summary: "Pick the right column type — integers, exact decimals, text, dates, booleans — and know how dialects differ.",
  est: 10,
  tags: ["types", "DECIMAL", "VARCHAR", "timestamp", "dialects"],
};

export default meta;
