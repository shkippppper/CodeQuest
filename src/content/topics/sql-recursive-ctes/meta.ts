import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-recursive-ctes",
  title: "Recursive CTEs",
  category: "sql-subqueries",
  difficulty: "senior",
  order: 4,
  summary: "A CTE that references itself — walk hierarchies and generate sequences with an anchor plus a recursive step.",
  est: 12,
  tags: ["recursive CTE", "WITH RECURSIVE", "hierarchy", "anchor"],
};

export default meta;
