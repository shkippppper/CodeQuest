import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "opaque-existential",
  title: "Opaque Types & Existentials (some vs any)",
  category: "types",
  difficulty: "senior",
  order: 4,
  summary: "some (one hidden concrete type, reverse generic) vs any (a boxed existential) — and when each is right.",
  est: 14,
  tags: ["some", "any", "opaque type", "existential", "boxing"],
};

export default meta;
