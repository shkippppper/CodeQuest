import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftui-lifecycle",
  title: "View Lifecycle & Identity",
  category: "ui",
  difficulty: "senior",
  order: 8,
  summary: "Structural vs explicit identity, what drives redraws and @State lifetime, and onAppear vs the task modifier.",
  est: 13,
  tags: ["SwiftUI", "identity", "lifecycle", "onAppear", "task"],
};

export default meta;
