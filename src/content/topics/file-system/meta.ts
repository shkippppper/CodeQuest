import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "file-system",
  title: "File System & FileManager",
  category: "foundation",
  difficulty: "mid",
  order: 5,
  summary: "Where your app is allowed to write, how FileManager reads and writes files, and how coordination keeps data safe.",
  est: 12,
  tags: ["FileManager", "sandbox", "URL", "file coordination", "NSFileProtectionType"],
};

export default meta;
