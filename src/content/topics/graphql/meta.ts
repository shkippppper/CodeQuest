import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "graphql",
  title: "GraphQL on iOS",
  category: "foundation",
  difficulty: "senior",
  order: 16,
  summary:
    "A query language that fetches exactly the fields you ask for: queries/mutations/subscriptions, the schema, Apollo iOS codegen, and normalized caching.",
  est: 13,
  tags: ["graphql", "apollo", "networking", "api"],
};

export default meta;
