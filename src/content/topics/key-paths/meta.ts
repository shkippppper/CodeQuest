import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "key-paths",
  title: "Key Paths",
  category: "types",
  difficulty: "mid",
  order: 8,
  summary: "Type-safe references to properties as values — reading, writing, using them as functions, and dynamic member lookup.",
  est: 12,
  tags: ["key path", "KeyPath", "WritableKeyPath", "dynamicMemberLookup", "generics"],
};

export default meta;
