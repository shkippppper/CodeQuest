import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "frame-bounds",
  title: "Frame vs Bounds",
  category: "ui",
  difficulty: "junior",
  order: 14,
  summary: "Two coordinate systems: frame (in the superview) vs bounds (in the view's own space), and how transforms affect them.",
  est: 10,
  tags: ["UIKit", "frame", "bounds", "coordinate system", "transform"],
};

export default meta;
