import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "observation",
  title: "The Observation Framework (@Observable)",
  category: "ui",
  difficulty: "mid",
  order: 4,
  summary: "The @Observable macro replaces ObservableObject with fine-grained, per-property change tracking.",
  est: 12,
  tags: ["@Observable", "Observation", "fine-grained", "@Bindable", "@Published"],
};

export default meta;
