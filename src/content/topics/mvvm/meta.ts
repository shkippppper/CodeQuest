import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "mvvm",
  title: "MVVM",
  category: "architecture",
  difficulty: "mid",
  order: 2,
  summary: "Adding a ViewModel to hold presentation logic and state — testable, UI-framework-agnostic, and bindable.",
  est: 13,
  tags: ["MVVM", "ViewModel", "binding", "testability", "presentation logic"],
};

export default meta;
