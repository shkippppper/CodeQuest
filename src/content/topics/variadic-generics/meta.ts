import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "variadic-generics",
  title: "Variadic Generics (Parameter Packs)",
  category: "types",
  difficulty: "senior",
  order: 11,
  summary:
    "One generic function or type that works over any NUMBER of type parameters, using each/repeat parameter packs.",
  est: 13,
  tags: ["generics", "parameter-packs", "variadic", "type-system"],
};

export default meta;
