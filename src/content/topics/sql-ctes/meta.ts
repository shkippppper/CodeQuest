import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-ctes",
  title: "Common Table Expressions",
  category: "sql-subqueries",
  difficulty: "mid",
  order: 3,
  summary: "Name a subquery with WITH to make complex queries readable and reuse a result within one statement.",
  est: 10,
  tags: ["CTE", "WITH", "readability", "subquery"],
};

export default meta;
