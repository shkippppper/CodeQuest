import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-isolation-levels",
  title: "Isolation Levels",
  category: "sql-dml-transactions",
  difficulty: "senior",
  order: 3,
  summary: "The four isolation levels and the read anomalies (dirty, non-repeatable, phantom) each one allows.",
  est: 13,
  tags: ["isolation levels", "dirty read", "phantom", "serializable"],
};

export default meta;
