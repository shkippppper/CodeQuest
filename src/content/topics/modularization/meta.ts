import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "modularization",
  title: "Modularization",
  category: "architecture",
  difficulty: "senior",
  order: 10,
  summary: "Splitting an app into feature and core modules (SPM) for build speed, boundaries, and team scale.",
  est: 13,
  tags: ["modularization", "SPM", "build time", "boundaries", "dependency graph"],
};

export default meta;
