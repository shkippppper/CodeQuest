import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "xcode-build",
  title: "The Xcode Build System",
  category: "tooling",
  difficulty: "mid",
  order: 2,
  summary: "How targets, schemes, configurations, build settings and build phases turn source into a shipped binary.",
  est: 12,
  tags: ["Xcode", "build settings", "xcconfig", "schemes", "workspaces"],
};

export default meta;
