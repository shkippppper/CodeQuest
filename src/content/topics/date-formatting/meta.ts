import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "date-formatting",
  title: "Date, Calendar & Formatters",
  category: "foundation",
  difficulty: "junior",
  order: 10,
  summary: "Date as a point in time, Calendar components, time zones, DateFormatter/ISO8601, the modern FormatStyle API, and localization.",
  est: 11,
  tags: ["Date", "Calendar", "TimeZone", "DateFormatter", "FormatStyle", "localization"],
};

export default meta;
