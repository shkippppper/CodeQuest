import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "core-data",
  title: "Core Data",
  category: "foundation",
  difficulty: "mid",
  order: 6,
  summary: "How the persistent container, managed object contexts, and fetch requests work together to store and query objects on disk.",
  est: 15,
  tags: ["Core Data", "NSManagedObjectContext", "NSFetchRequest", "NSFetchedResultsController", "migrations"],
};

export default meta;
