import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "dependency-injection",
  title: "Dependency Injection",
  category: "architecture",
  difficulty: "mid",
  order: 9,
  summary: "Passing dependencies in instead of creating them — for decoupling, testability, and flexible composition.",
  est: 12,
  tags: ["dependency injection", "constructor injection", "testability", "service locator"],
};

export default meta;
