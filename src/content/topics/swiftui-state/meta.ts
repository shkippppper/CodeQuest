import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftui-state",
  title: "State Management (@State & @Binding)",
  category: "ui",
  difficulty: "mid",
  order: 2,
  summary: "Source of truth, @State for owned state, @Binding for shared two-way access, and the $ projected value.",
  est: 13,
  tags: ["SwiftUI", "@State", "@Binding", "source of truth", "projected value"],
};

export default meta;
