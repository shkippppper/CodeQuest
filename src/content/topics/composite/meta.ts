import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "composite",
  title: "Composite",
  category: "patterns",
  difficulty: "senior",
  order: 8,
  summary: "Treating a single object and a tree of objects through the same interface — the pattern quietly underneath every SwiftUI view.",
  est: 12,
  tags: ["composite", "tree", "recursion", "SwiftUI", "structural pattern"],
};

export default meta;
