import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "signing-distribution",
  title: "Provisioning, Signing & Distribution",
  category: "tooling",
  difficulty: "mid",
  order: 3,
  summary: "How certificates, App IDs, and provisioning profiles let a build run on a device or ship through TestFlight and the App Store.",
  est: 13,
  tags: ["code signing", "provisioning profile", "certificate", "TestFlight", "App Store"],
};

export default meta;
