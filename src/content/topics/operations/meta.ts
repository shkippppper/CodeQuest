import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "operations",
  title: "Operations & OperationQueue",
  category: "concurrency",
  difficulty: "mid",
  order: 2,
  summary: "A higher-level, object-oriented layer over GCD: dependencies, cancellation, and reusable units of work.",
  est: 12,
  tags: ["Operation", "OperationQueue", "dependencies", "cancellation"],
};

export default meta;
