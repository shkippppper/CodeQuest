import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sendable",
  title: "Sendable & Data Races",
  category: "concurrency",
  difficulty: "senior",
  order: 6,
  summary: "The Sendable protocol — how the compiler proves a value is safe to cross concurrency boundaries.",
  est: 13,
  tags: ["Sendable", "data race", "@Sendable", "@unchecked", "concurrency safety"],
};

export default meta;
