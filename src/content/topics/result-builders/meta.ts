import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "result-builders",
  title: "Result Builders",
  category: "types",
  difficulty: "senior",
  order: 7,
  summary: "The @resultBuilder feature that turns a block of statements into one value — the magic behind ViewBuilder.",
  est: 13,
  tags: ["result builder", "@resultBuilder", "buildBlock", "ViewBuilder", "DSL"],
};

export default meta;
