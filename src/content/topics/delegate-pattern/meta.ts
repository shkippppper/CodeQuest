import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "delegate-pattern",
  title: "Delegate & DataSource",
  category: "patterns",
  difficulty: "junior",
  order: 9,
  summary: "Cocoa's delegation idiom: a protocol, a weak delegate property, and how it compares to closures and Combine.",
  est: 10,
  tags: ["delegate", "dataSource", "protocol", "weak", "cocoa"],
};

export default meta;
