import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftui-animations",
  title: "Animations & Transitions",
  category: "ui",
  difficulty: "mid",
  order: 9,
  summary: "Implicit vs explicit animation, transitions, matchedGeometryEffect, and custom Animatable values.",
  est: 13,
  tags: ["SwiftUI", "animation", "withAnimation", "transition", "matchedGeometryEffect"],
};

export default meta;
