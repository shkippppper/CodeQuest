import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "regex",
  title: "Regex & RegexBuilder",
  category: "language",
  difficulty: "mid",
  order: 14,
  summary:
    "Swift's modern typed regex: literals, runtime Regex(String), typed captures, and the readable RegexBuilder DSL.",
  est: 13,
  tags: ["regex", "regexbuilder", "parsing", "strings"],
};

export default meta;
