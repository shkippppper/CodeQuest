import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "swiftui-modifiers",
  title: "Custom Modifiers & ViewBuilders",
  category: "ui",
  difficulty: "senior",
  order: 10,
  summary: "Reusable ViewModifiers, @ViewBuilder functions, conditional content, and child→parent data via PreferenceKeys.",
  est: 12,
  tags: ["ViewModifier", "@ViewBuilder", "PreferenceKey", "reuse", "SwiftUI"],
};

export default meta;
