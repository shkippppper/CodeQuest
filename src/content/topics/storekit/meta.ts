import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "storekit",
  title: "StoreKit 2 & In-App Purchases",
  category: "foundation",
  difficulty: "mid",
  order: 13,
  summary:
    "Selling in-app: product types, the async purchase flow, transaction verification, current entitlements, and the updates listener.",
  est: 13,
  tags: ["storekit", "iap", "subscriptions", "transactions"],
};

export default meta;
