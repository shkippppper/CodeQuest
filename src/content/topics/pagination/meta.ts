import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "pagination",
  title: "Pagination Strategies",
  category: "system-design",
  difficulty: "mid",
  order: 7,
  summary:
    "Loading a long list a page at a time: offset vs cursor paging, when to prefetch, how to dedupe, and keeping the cache coherent.",
  est: 12,
  tags: ["pagination", "cursor", "prefetch", "cache", "system design"],
};

export default meta;
