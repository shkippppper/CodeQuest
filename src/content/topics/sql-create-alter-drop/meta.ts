import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-create-alter-drop",
  title: "CREATE, ALTER & DROP",
  category: "sql-ddl",
  difficulty: "junior",
  order: 1,
  summary: "Define tables with CREATE, change their shape with ALTER, and remove them with DROP — plus how DDL differs from deleting rows.",
  est: 10,
  tags: ["CREATE TABLE", "ALTER TABLE", "DROP", "TRUNCATE"],
};

export default meta;
