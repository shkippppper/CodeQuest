import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "sql-inner-outer-joins",
  title: "INNER & OUTER Joins",
  category: "sql-joins",
  difficulty: "mid",
  order: 1,
  summary: "Combine rows from two tables on a matching key — INNER keeps matches only, OUTER keeps unmatched rows too.",
  est: 12,
  tags: ["JOIN", "INNER JOIN", "LEFT JOIN", "OUTER JOIN"],
};

export default meta;
