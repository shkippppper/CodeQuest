import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "test-strategy",
  title: "Test Strategy & Coverage",
  category: "testing",
  difficulty: "senior",
  order: 8,
  summary: "Shaping a test suite with the testing pyramid, treating coverage as a signal rather than a target, and keeping suites healthy in CI over time.",
  est: 12,
  tags: ["testing pyramid", "coverage", "CI", "test strategy", "maintenance"],
};

export default meta;
