import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "decorator",
  title: "Decorator",
  category: "patterns",
  difficulty: "mid",
  order: 5,
  summary: "Wrapping an object in same-protocol layers to add behavior at runtime, without subclassing.",
  est: 11,
  tags: ["decorator", "composition", "protocol", "wrapper", "structural pattern"],
};

export default meta;
