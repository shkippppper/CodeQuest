import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "ds-heaps",
  title: "Heaps & Priority Queues",
  category: "cs-fundamentals",
  difficulty: "senior",
  order: 8,
  summary: "The array-backed binary tree behind priority queues, top-K problems, and heapsort.",
  est: 14,
  tags: ["heap", "priority queue", "top-K", "heapsort"],
};

export default meta;
