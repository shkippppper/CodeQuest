import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "extensions",
  title: "Extensions",
  category: "types",
  difficulty: "junior",
  order: 5,
  summary: "Add methods, computed properties, and protocol conformances to existing types — even ones you don't own.",
  est: 11,
  tags: ["extension", "computed property", "conformance", "constrained extension"],
};

export default meta;
