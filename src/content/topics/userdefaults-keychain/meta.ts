import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "userdefaults-keychain",
  title: "UserDefaults & Keychain",
  category: "foundation",
  difficulty: "junior",
  order: 4,
  summary: "Persisting small settings with UserDefaults and @AppStorage, and storing secrets safely in the Keychain.",
  est: 10,
  tags: ["UserDefaults", "Keychain", "@AppStorage", "persistence", "security"],
};

export default meta;
