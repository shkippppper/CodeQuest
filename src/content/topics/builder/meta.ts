import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "builder",
  title: "Builder",
  category: "patterns",
  difficulty: "mid",
  order: 3,
  summary: "Assembling a complex object step by step — fluent method chains, result-builder DSLs, and when default arguments already do the job.",
  est: 10,
  tags: ["builder", "fluent interface", "result builder", "DSL", "creational"],
};

export default meta;
