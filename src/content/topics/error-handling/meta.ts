import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "error-handling",
  title: "Error Handling",
  category: "language",
  difficulty: "mid",
  order: 10,
  summary: "throws / try / catch, try? and try!, defer, the Result type, and choosing errors vs optionals vs crashes.",
  est: 14,
  tags: ["throws", "try", "catch", "defer", "Result", "Error"],
};

export default meta;
