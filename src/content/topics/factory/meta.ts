import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "factory",
  title: "Factory & Abstract Factory",
  category: "patterns",
  difficulty: "mid",
  order: 2,
  summary: "Letting a function or type decide what to build, so callers depend on a protocol instead of a concrete type.",
  est: 10,
  tags: ["factory method", "abstract factory", "static factory", "creational", "decoupling"],
};

export default meta;
