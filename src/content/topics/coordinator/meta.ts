import type { TopicMeta } from "../../types";

const meta: TopicMeta = {
  id: "coordinator",
  title: "Coordinator Pattern",
  category: "architecture",
  difficulty: "mid",
  order: 8,
  summary: "Pull navigation out of view controllers into dedicated coordinator objects that own app flow.",
  est: 12,
  tags: ["coordinator", "navigation", "flow", "child coordinators", "decoupling"],
};

export default meta;
